services:
  - type: web
    name: atmos-support-rag
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt fastapi uvicorn
    startCommand: python app.py
    envVars:
      - key: PORT
        value: 10000
